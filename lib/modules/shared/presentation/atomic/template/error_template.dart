import 'package:flutter/material.dart';

class ErrorTemplate extends StatelessWidget {
  const ErrorTemplate({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Column(),
    );
  }
}
